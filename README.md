# H-UI (Hysteria 2) Docker Setup

Веб-панель H-UI для управления Hysteria 2 прокси-сервером с автоматическим SSL.

## 📋 Компоненты

- **H-UI** - Веб-панель для управления Hysteria 2
- **Hysteria 2** - Высокопроизводительный прокси-протокол на базе QUIC
- **Certbot** - Автоматическое получение Let's Encrypt сертификатов

## 🚀 Быстрая установка

### Требования

- VPS с Ubuntu 20.04+ / Debian 11+
- Доменное имя, направленное на IP сервера
- Открытые порты: 80, 443 (TCP и UDP), 8081

### Установка

1. Загрузите файлы на сервер:
```bash
cd /root
git clone <repo> h-ui
cd h-ui
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x install.sh
```

3. Запустите установку:
```bash
sudo ./install.sh
```

## 📁 Структура проекта

```
.
├── docker-compose.yml    # Docker Compose конфигурация
├── install.sh           # Скрипт установки
├── .env                 # Переменные окружения
├── certbot/             # SSL сертификаты
└── h-ui-data/           # Данные H-UI
```

## 🔧 Настройка

### Порты

| Сервис | Порт | Протокол | Описание |
|--------|------|----------|----------|
| H-UI Panel | 8081 | TCP | Веб-панель управления |
| Hysteria 2 | 443 | UDP | Прокси-соединения |
| HTTP | 80 | TCP | Редирект на HTTPS |

### Файерволл

```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 443/udp
ufw allow 8081/tcp
ufw reload
```

## 📱 Использование

### Вход в панель

- **URL:** https://your-domain.com:8081
- **Логин:** admin
- **Пароль:** admin

**ВАЖНО:** Смените пароль после первого входа!

### Создание пользователей

1. Войдите в панель H-UI
2. Перейдите в раздел "Users"
3. Нажмите "Add User"
4. Укажите имя и лимиты
5. Скопируйте конфиг для клиента

## 🛠 Управление

```bash
# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d
```

## 📝 Лицензия

MIT License
