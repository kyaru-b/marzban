# Hysteria 2 + Marzban + Nginx Docker Setup

Полный Docker-стек для развертывания прокси-сервера с Hysteria 2, панелью управления Marzban и обратным проксированием через Nginx с автоматическим SSL.

## 📋 Компоненты

- **Marzban** - Панель управления прокси-пользователями с поддержкой Xray
- **Hysteria 2** - Высокопроизводительный прокси-протокол на базе QUIC
- **Nginx** - Обратный прокси с SSL/TLS терминацией
- **Certbot** - Автоматическое получение и обновление Let's Encrypt сертификатов

## 🚀 Быстрая установка

### Требования

- VPS с Ubuntu 20.04+ / Debian 11+
- Доменное имя, направленное на IP сервера
- Открытые порты: 80, 443 (TCP и UDP)

### Установка

1. Загрузите файлы на сервер:
```bash
# Клонируйте или скопируйте все файлы в директорию
cd /opt/proxy-stack
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x install.sh
```

3. Запустите установку:
```bash
sudo ./install.sh
```

4. Следуйте инструкциям:
   - Введите ваш домен (например: proxy.example.com)
   - Введите email для SSL сертификата
   - Задайте имя пользователя и пароль администратора Marzban
   - Задайте пароль для Hysteria 2

## 📁 Структура проекта

```
.
├── docker-compose.yml          # Основной файл Docker Compose
├── install.sh                  # Скрипт установки
├── uninstall.sh               # Скрипт удаления
├── .env.template              # Шаблон переменных окружения
├── .env                       # Переменные окружения (создается при установке)
├── nginx/
│   └── conf.d/
│       ├── default.conf.template  # Шаблон конфигурации Nginx
│       ├── default.conf          # Конфигурация Nginx (создается при установке)
│       └── initial.conf          # Начальная конфигурация для ACME
├── hysteria2/
│   ├── Dockerfile             # Dockerfile для Hysteria 2
│   ├── entrypoint.sh          # Скрипт запуска Hysteria
│   └── config/
│       ├── config.yaml.template  # Шаблон конфигурации Hysteria
│       └── config.yaml          # Конфигурация Hysteria (создается при установке)
├── xray-config/
│   └── xray_config.json       # Конфигурация Xray для Marzban
├── certbot/                   # SSL сертификаты (создается при установке)
│   ├── www/
│   └── conf/
└── marzban-data/              # Данные Marzban (создается при установке)
```

## 🔧 Настройка

### Порты

| Сервис | Порт | Протокол | Описание |
|--------|------|----------|----------|
| Nginx | 80 | TCP | HTTP (редирект на HTTPS) |
| Nginx | 443 | TCP | HTTPS (Marzban панель) |
| Hysteria 2 | 443 | UDP | Основной порт Hysteria |
| Hysteria 2 | 8443 | UDP | Альтернативный порт |
| Marzban | 8080 | TCP | Внутренний порт панели |

### Файерволл

```bash
# UFW
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 443/udp
ufw allow 8443/udp
ufw reload

# iptables
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
iptables -A INPUT -p udp --dport 443 -j ACCEPT
iptables -A INPUT -p udp --dport 8443 -j ACCEPT
```

## 📱 Подключение клиентов

### Hysteria 2 Client Config

```yaml
server: your-domain.com:443
auth: your-password
bandwidth:
  up: 100 mbps
  down: 100 mbps
tls:
  sni: your-domain.com
  insecure: false
socks5:
  listen: 127.0.0.1:1080
http:
  listen: 127.0.0.1:8080
```

### Marzban

Откройте `https://your-domain.com` в браузере и войдите с учетными данными администратора.

## 🛠 Управление

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f marzban
docker-compose logs -f hysteria2
docker-compose logs -f nginx
```

### Перезапуск сервисов

```bash
docker-compose restart
docker-compose restart marzban
docker-compose restart hysteria2
```

### Остановка

```bash
docker-compose down
```

### Запуск

```bash
docker-compose up -d
```

### Обновление

```bash
docker-compose pull
docker-compose up -d
```

## 🔐 Безопасность

1. **Удалите файл credentials.txt** после сохранения учетных данных
2. Используйте сложные пароли
3. Регулярно обновляйте контейнеры
4. Настройте файерволл
5. Рассмотрите использование fail2ban

## ❌ Удаление

```bash
chmod +x uninstall.sh
sudo ./uninstall.sh
```

## 🐛 Устранение неполадок

### SSL сертификат не получен

- Убедитесь, что домен указывает на IP сервера
- Проверьте, что порт 80 открыт
- Проверьте DNS пропагацию: `nslookup your-domain.com`

### Marzban не загружается

```bash
docker-compose logs marzban
docker-compose restart marzban
```

### Hysteria 2 не работает

```bash
docker-compose logs hysteria2
# Проверьте, что UDP порт 443 открыт
```

## 📝 Лицензия

MIT License
